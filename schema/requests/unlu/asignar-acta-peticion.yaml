---
id_peticion:
    validators:
        integer:
            label: id
            message: VALIDATE.INTEGER
archivo:
    validators:
        required:
            label: "&UNLU.PETITION.CERTIFICATE"
            message: VALIDATE.REQUIRED
        regex:
            regex: .*\.pdf
            message: UNLU.CERTIFICATE.FILE.PDF_ONLY